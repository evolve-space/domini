server {
    server_name domini.ksabitova.dev;

    server_tokens off;

    # Embedded report iframe: no COEP or CSP to allow the report HTML to load its own resources.
    # Having its own add_header prevents inheriting the parent location's headers (nginx behaviour).
    location ~ ^/scans/[0-9]+/report(/token)?$ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        add_header Cross-Origin-Resource-Policy "same-origin" always;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        add_header Cross-Origin-Opener-Policy "same-origin" always;
        add_header Cross-Origin-Resource-Policy "same-origin" always;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/domini.ksabitova.dev/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/domini.ksabitova.dev/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}
server {
    if ($host = domini.ksabitova.dev) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name domini.ksabitova.dev;
    return 404; # managed by Certbot


}
